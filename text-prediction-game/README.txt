# text-prediction-game

To run the game,

1. Input the following in the terminal:
$ php -S localhost:8888

2. In your browser, access the url:
Localhost:8888

The data will save locally in the data/ directory.